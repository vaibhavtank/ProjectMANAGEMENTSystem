package com.mcit.pms.service;

import com.mcit.pms.model.Authority;

public interface AuthorityService {
    void insertAuthorityOfUser(Authority authority);

    void updateUser(Authority authority);
}
